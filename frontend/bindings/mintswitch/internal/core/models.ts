// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Create as $Create } from "@wailsio/runtime";

/**
 * ApplyResult summarizes the outcome of [ToolAdapter.Apply].
 */
export class ApplyResult {
    /**
     * ChangedPath is the tool config file that was written.
     */
    "changed_path"?: string;

    /**
     * BackupPath is the backup created before the change, if any.
     */
    "backup_path"?: string;

    /**
     * Message is a human-readable summary, safe to display (no secrets).
     */
    "message"?: string;

    /** Creates a new ApplyResult instance. */
    constructor($$source: Partial<ApplyResult> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new ApplyResult instance from a string or object.
     */
    static createFrom($$source: any = {}): ApplyResult {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new ApplyResult($$parsedSource as Partial<ApplyResult>);
    }
}

/**
 * MCPResult summarizes the outcome of an inject/remove operation. It mirrors
 * [ApplyResult] and carries only display-safe fields (never a secret).
 */
export class MCPResult {
    /**
     * ChangedPath is the tool config file that was written.
     */
    "changed_path"?: string;

    /**
     * BackupPath is the backup created/used, if any.
     */
    "backup_path"?: string;

    /**
     * Message is a human-readable summary, safe to display (no secrets).
     */
    "message"?: string;

    /** Creates a new MCPResult instance. */
    constructor($$source: Partial<MCPResult> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new MCPResult instance from a string or object.
     */
    static createFrom($$source: any = {}): MCPResult {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new MCPResult($$parsedSource as Partial<MCPResult>);
    }
}

/**
 * Profile is a single OpenAI-compatible endpoint configuration that the user
 * wants to apply to one or more AI coding tools.
 * 
 * API keys live in this struct; callers must never write APIKey to logs.
 */
export class Profile {
    /**
     * Label is an optional human-friendly name for the profile.
     */
    "label"?: string;

    /**
     * APIKey is the secret bearer token for the endpoint. Required.
     */
    "api_key": string;

    /**
     * BaseURL is the OpenAI-compatible base URL (http or https). Required.
     */
    "base_url": string;

    /**
     * Models is the user's saved set of selectable model identifiers. The
     * currently selected one is Model, which must be a member when Models is
     * non-empty. Adapters never read Models; they consume only Model.
     */
    "models"?: string[];

    /**
     * Model is the currently selected model identifier and the single value
     * adapters write to tool configs. Required.
     */
    "model": string;

    /**
     * SmallFastModel is an optional secondary model used by some tools for
     * lightweight/background tasks. It need not be a member of Models.
     */
    "small_fast_model"?: string;

    /** Creates a new Profile instance. */
    constructor($$source: Partial<Profile> = {}) {
        if (!("api_key" in $$source)) {
            this["api_key"] = "";
        }
        if (!("base_url" in $$source)) {
            this["base_url"] = "";
        }
        if (!("model" in $$source)) {
            this["model"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Profile instance from a string or object.
     */
    static createFrom($$source: any = {}): Profile {
        const $$createField3_0 = $$createType0;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("models" in $$parsedSource) {
            $$parsedSource["models"] = $$createField3_0($$parsedSource["models"]);
        }
        return new Profile($$parsedSource as Partial<Profile>);
    }
}

/**
 * RestoreResult summarizes the outcome of [ToolAdapter.Restore].
 */
export class RestoreResult {
    /**
     * ChangedPath is the tool config file that was restored or removed.
     */
    "changed_path"?: string;

    /**
     * BackupPath is the backup that was used to restore, if any.
     */
    "backup_path"?: string;

    /**
     * Message is a human-readable summary, safe to display (no secrets).
     */
    "message"?: string;

    /** Creates a new RestoreResult instance. */
    constructor($$source: Partial<RestoreResult> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new RestoreResult instance from a string or object.
     */
    static createFrom($$source: any = {}): RestoreResult {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new RestoreResult($$parsedSource as Partial<RestoreResult>);
    }
}

// Private type creation functions
const $$createType0 = $Create.Array($Create.Any);
